class Node {
  constructor(data, next, prev) {
    this.data = data;
    this.next = next;
    this.prev = prev;
  }
}

class SinglyLinkedList {
  constructor() {
    //create null initialized list
    this.list = null;
  }

  insert = () => {
    //
  };

  delete = (data) => {
    //
  };

  reverse = () => {
    //
  };

  display = (data) => {
    //
  };
}